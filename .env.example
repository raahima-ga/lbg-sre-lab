# Copy to .env and fill in. NEVER commit .env -- it is gitignored.
#
# Create the token in Dynatrace: Ctrl+K -> "Access tokens" -> Generate new token
# Name: lab-ingest
# Scopes (all three required):
#   - Ingest OpenTelemetry traces
#   - Ingest metrics
#   - Ingest logs
#
# No quotes around the value.
DT_ENDPOINT=https://bcj02052.live.dynatrace.com
DT_API_TOKEN=
